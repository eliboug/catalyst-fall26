---
import BaseLayout from '../layouts/BaseLayout.astro';
import { field, safeNext } from '../lib/auth';

export const prerender = false;

const next = safeNext(Astro.url.searchParams.get('next'));
if (Astro.locals.user) return Astro.redirect(next);

let error =
  Astro.url.searchParams.get('confirm') === 'failed'
    ? 'That confirmation link has expired or was already used. Try signing in.'
    : '';
let email = '';

if (Astro.request.method === 'POST') {
  const form = await Astro.request.formData();
  email = field(form, 'email').toLowerCase();
  const password = form.get('password');

  if (!email || typeof password !== 'string' || !password) {
    error = 'Enter your email and password.';
  } else {
    const { error: authError } = await Astro.locals.supabase.auth.signInWithPassword({ email, password });
    if (!authError) return Astro.redirect(next);
    error =
      authError.code === 'email_not_confirmed'
        ? 'Confirm your email first. Check your Yale inbox for the link.'
        : 'That email and password don’t match an account.';
  }
}
---

<BaseLayout title="Sign in">
  <section class="page auth-page">
    <div class="slash" aria-hidden="true"></div>
    <div class="auth-body">
      <h2>Sign in</h2>
      <form method="post" class="form" novalidate>
        <label class="field">
          <span class="label">Yale email</span>
          <input class="input" type="email" name="email" value={email} autocomplete="email" required />
        </label>
        <label class="field">
          <span class="label">Password</span>
          <input class="input" type="password" name="password" autocomplete="current-password" required />
        </label>
        {error && <p class="form-error" role="alert">{error}</p>}
        <div>
          <button type="submit" class="btn btn--primary">Sign in</button>
        </div>
      </form>
      <p class="form-note">New here? <a href={`/signup${next !== '/' ? `?next=${encodeURIComponent(next)}` : ''}`}>Create an account</a></p>
    </div>
  </section>
</BaseLayout>
